import React from "react";
import useStyles from "./Future,styles";

const Future: React.FC<{}> = () => {
  const classes = useStyles();
  return <div className={classes.root}>Future</div>;
};

export default Future;
